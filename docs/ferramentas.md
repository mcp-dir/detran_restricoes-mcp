# Ferramentas

DETRAN: Restrições (Unificada) expõe 1 ferramenta (todas somente leitura).

### 1. `detran_restricoes_consultar`
**Input**: `uf`, `placa`, `renavam`, `chassi`, `cpf` (opcional), `cnpj` (opcional), `login_cpf` (opcional), `login_cnpj` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

DETRAN: Restrições (Unificada), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN: Restrições (Unificada)
```
